import React from 'react';
import { Typography } from '@mui/material';

const ContactUs = () => {
	return (
		<div>
			<Typography style={{ minHeight: '50vh', fontSize: '3rem' }}>
				Contact Us
			</Typography>
		</div>
	);
};

export default ContactUs;
